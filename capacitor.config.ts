import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
	appId: 'dev.darekaze.campion',
	appName: 'campion',
	webDir: 'dist',
	bundledWebRuntime: false,
}

export default config
